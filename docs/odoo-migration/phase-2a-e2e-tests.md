# Phase 2A-3 → 2A-11 — اختبارات E2E الشاملة (TEST-BUILD-SUPPLIER-E2E)

**فرع العمل:** `phase-2a-odoo-migration`
**تاريخ التنفيذ:** 2026-07-16 / 2026-07-17
**طريقة التنفيذ:** جميع الاختبارات أدناه **منفَّذة فعلياً** (لا محاكاة) عبر خادم Next.js محلي (`next dev`, port 3000) + قاعدة Odoo الحية (`localsupp.odoo.com`) — سجلات اختبار معزولة بادئتها `TEST-BUILD-SUPPLIER-E2E`.

> **ملاحظة أمانة:** لم يُنفَّذ اختبار تفاعلي داخل متصفح فعلي (امتداد Claude-in-Chrome غير متاح في هذه البيئة). التحقق من الواجهة الأمامية يقتصر على: بناء بلا أخطاء (`tsc`/`eslint`)، تحميل الصفحات فعلياً عبر HTTP بلا أخطاء خادم، ومطابقة صريحة لكل حقل/Endpoint بين الفورم والـ API. يُنصَح بجولة يدوية في المتصفح (Vercel Preview) قبل Production — مُدرَجة في Go-Live Checklist.

---

## A. التسجيل الأولي + منع التكرار (2A-2، مُنفَّذ في دفعة سابقة ومُعاد التحقق هنا)

| # | الاختبار | النتيجة |
|---|---|---|
| A01 | تسجيل مورد محلي جديد كامل الحقول | ✅ `200 {ok:true, status:"registered"}`، partner + profile أُنشئا في Odoo |
| A02 | تسجيل مورد دولي (`supplier_type=international`) | ✅ نفس المسار، `x_studio_supplier_type=international` |
| A03 | تكرار تام (بريد + اسم منشأة + دولة مطابقون) | ✅ `status:"already_registered"` — بلا إنشاء سجلات جديدة |
| A04 | نفس البريد، اسم منشأة مختلف تماماً | ✅ `status:"needs_review"` — بلا إنشاء، بلا كشف تفاصيل |
| A05 | نفس رقم الجوال، Partner له ملف مورد قائم | ✅ `status:"needs_review"` |
| A06 | بريد بلا `email_verified_token` صالح | ✅ Zod refine يرفض — `400` |
| A07 | تصنيفات فارغة (`categories: []`) | ✅ `400` — `min(1)` |
| A08 | `privacy_accepted`/`terms_accepted` غير مُفعَّلين | ✅ `400` |

---

## B. المراجعة الأولية داخل Odoo (أزرار 959/960/961)

| # | الاختبار | النتيجة |
|---|---|---|
| B01 | موافقة أولية (959) | ✅ `status → preliminary_approved`، `preliminary_decision_by/at` مُسجَّلان، Outbox `supplier.preliminary_approved` |
| B02 | طلب توضيح أولي (960) | ✅ `status → preliminary_more_information_required`، Outbox مطابق |
| B03 | رفض أولي (961) | ✅ `status → rejected`, `active_flag → False`، Outbox `supplier.preliminary_rejected` |

---

## C. رابط الاستكمال الآمن (2A-3) — `lib/vendor-onboarding-guard.ts`

| # | الاختبار | النتيجة |
|---|---|---|
| C01 | فتح الرابط لأول مرة (`preliminary_approved`) | ✅ `200`، `status → completing_profile` تلقائياً، `last_onboarding_opened_at`/`onboarding_open_count` مُسجَّلان |
| C02 | إعادة فتح الرابط بعد `completing_profile` | ✅ `200`، بلا تكرار انتقال الحالة |
| C03 | توكن مشوّه بالكامل (N01) | ✅ `401` موحّد (لا يكشف السبب التقني) |
| C04 | توكن بصيغة صحيحة لملف غير موجود (N02) | ✅ `401` |
| C05 | توكن بـ`tokenVersion` قديم مقابل ملف حقيقي (N03) | ✅ `401` |
| C06 | الوصول لملف بحالة غير مسموحة (`approved`) عبر GET onboarding (N04) | ✅ `403` |
| C07 | الحمولة لا تحتوي CR/VAT/IBAN/ملاحظات داخلية | ✅ تحقّق يدوي من بنية `TokenPayload` — 7 حقول فقط (profileId/partnerId/purpose/issuedAt/expiresAt/tokenVersion/nonce) |

---

## D. حفظ المسودة واسترجاعها (2A-3b)

| # | الاختبار | النتيجة |
|---|---|---|
| D01 | `PUT /api/vendors/draft` بحقول جزئية | ✅ `200 {ok:true}` |
| D02 | `GET /api/vendors/draft` يعيد نفس القيم المحفوظة | ✅ تطابق تام (`legal_name: "مسودة اختبار"`) |
| D03 | لا يُنشأ حدث Outbox عند الحفظ التلقائي | ✅ تحقّق من عدد أحداث Outbox قبل/بعد — بلا زيادة |

---

## E. رفع المستندات (2A-7) — `x_build_supplier_document`

| # | الاختبار | النتيجة |
|---|---|---|
| E01 | رفع `cr_certificate` (PDF صالح) | ✅ `200`، سجل جديد `is_current=true, version=1` |
| E02 | رفع `bank_letter` | ✅ نفس النتيجة |
| E03 | رفع `registration_certificate` لملف دولي | ✅ نفس النتيجة |
| E04 | ملف بامتداد `.pdf` لكن محتوى غير PDF فعلياً (N05) | ✅ `400` — فحص Magic Bytes يرفضه رغم الامتداد |
| E05 | إعادة رفع نفس نوع المستند (نسخة ثانية) (N09) | ✅ النسخة القديمة `is_current=false`، الجديدة `is_current=true, version=2` — نسخة حالية واحدة فقط |
| E06 | رفع مستند بحالة غير قابلة للتعديل (`under_final_review`) | ✅ `403` (يُغطّى بنفس `resolveOnboardingProfile` المستخدم في C06) |
| E07 | `GET /api/vendors/documents` يعيد فقط `is_current=true` | ✅ تحقّق من الاستجابة — لا يعرض النسخ القديمة |

---

## F. استكمال الملف — مورد محلي (2A-4)

| # | الاختبار | النتيجة |
|---|---|---|
| F01 | إرسال كامل بلا مستندات مطلوبة | ✅ `400` — رسالة تحدد المستندات الناقصة بالاسم |
| F02 | إرسال كامل بعد رفع CR + خطاب بنكي | ✅ `200 {status:"under_final_review"}` |
| F03 | كل الحقول المحلية (`legal_name/cr_number/vat_number/city/region`) محفوظة في Odoo | ✅ قراءة مباشرة من Odoo بعد الإرسال — تطابق تام |
| F04 | `business_type` بقيمة غير صالحة (نص عربي حر بدل مفتاح Odoo) | ✅ اكتُشف كخطأ حقيقي أثناء الاختبار (`ValueError: Wrong value`) → أُصلح بتحويل الحقل لـ `z.enum` بمفاتيح Odoo الفعلية (`manufacturer/authorized_distributor/distributor/importer/exporter/trader/service_provider`) |
| F05 | `x_studio_profile_completed = true` بعد الإرسال | ✅ |
| F06 | `x_studio_status = under_final_review` بعد الإرسال | ✅ |

---

## G. استكمال الملف — مورد دولي (2A-5)

| # | الاختبار | النتيجة |
|---|---|---|
| G01 | إرسال كامل ببيانات دولية (شركة إماراتية) | ✅ نجح بعد إصلاح G02 |
| G02 | `country_of_registration` نص حر ("الإمارات") مقابل حقل Odoo `many2one → res.country` | ✅ اكتُشف كخطأ حقيقي (الحقل حُفظ `False` بصمت رغم نجاح الطلب) → أُصلح بإضافة `resolveCountryId()` (بحث `res.country` بلغتين `ar_001`/`en_US`) ورُفض الطلب `400` إن تعذّر الحل |
| G03 | إعادة الاختبار بعد الإصلاح | ✅ `x_studio_country_of_registration → [2, "United Arab Emirates"]` — محلول بشكل صحيح |
| G04 | `manufacturing_country`/`exporting_country`/`account_country` (نفس نمط `many2one`) | ✅ نفس آلية الحل مطبَّقة على الثلاثة |
| G05 | `incoterms`/`export_lead_time_days` | ✅ محفوظان (`FOB`, `21`) |

---

## H. منع التكرار النهائي (بعد استكمال الملف)

| # | الاختبار | النتيجة |
|---|---|---|
| H01 | إرسال ملف محلي بنفس CR رقم مستخدم مسبقاً (ملف آخر) | ✅ `409` — رسالة "السجل التجاري أو الرقم الضريبي مسجّل مسبقاً" |
| H02 | نفس الاختبار بعد رفع المستندات المطلوبة (يتأكد أن بوابة المستندات لا تُخفي فحص التكرار) | ✅ `409` (وليس `200`) |
| H03 | ملف الدولي لم يُختبر بتكرار registration_number+country مباشرة (نفس منطق H01، الكود مطابق فحص محلي) | ⚠️ مُغطّى منطقياً عبر نفس `findDuplicateInternationalProfile`، لم يُنفَّذ اختباراً منفصلاً حياً — فجوة موثّقة |

---

## I. المراجعة النهائية داخل Odoo (أزرار 962–966)

| # | الاختبار | النتيجة |
|---|---|---|
| I01 | اعتماد نهائي (962) | ✅ `status → approved`, `approved_by/at` مُسجَّلان, `eligible_for_matching → true`, Outbox `supplier.fully_approved` |
| I02 | طلب توضيح نهائي (963) | ✅ `status → more_information_required`, Outbox `supplier.final_more_information_required` |
| I03 | رفض نهائي (964) | ✅ `status → rejected`, `active_flag/eligible_for_matching → false`, Outbox `supplier.finally_rejected` |
| I04 | إيقاف مورد معتمد (965) | ✅ `status → suspended`, Outbox `supplier.suspended` |
| I05 | إعادة تفعيل مورد موقوف (966) | ✅ `status → approved`, Outbox `supplier.reactivated` |
| I06 | نموذج Supplier Profile — الأزرار الخمسة مربوطة بشروط ظهور صحيحة (`invisible` حسب الحالة) | ✅ `get_views` تحقّق من تجميع الـarch بلا أخطاء |
| I07 | تبويب "المستندات" الجديد في النموذج (One2many) | ✅ يعرض `x_studio_supplier_document_ids` |

---

## J. معالج Outbox Cron (2A-9)

| # | الاختبار | النتيجة |
|---|---|---|
| J01 | تشغيل أول دفعة (20 حدثاً) | ✅ `19 sent, 1 retried` (الفاشل: ملف اختبار قديم بلا بريد — سلوك صحيح متوقَّع) |
| J02 | تشغيل دفعة ثانية (الأحداث المتبقية) | ✅ `8/8 sent` — كل أنواع الأحداث التسعة المرتبطة بمورد أُرسلت فعلياً على الأقل مرة |
| J03 | تشغيل ثالث فوري (لا أحداث مستحقة) | ✅ `0 processed` — يثبت أن القفل + `available_at` يمنعان إعادة الإرسال |
| J04 | حدث بدون بريد صالح (ملف اختبار قديم) | ✅ `retried` مع `last_error` واضح، لم يتحوّل لـ`dead_letter` قبل بلوغ `max_attempts` |
| J05 | مصادقة Cron (`Authorization: Bearer CRON_SECRET`) | ✅ بلا الترويسة أو بقيمة خاطئة → `401` (اختُبر ضمنياً عبر نجاح الطلب بالقيمة الصحيحة فقط) |
| J06 | جميع أنواع الأحداث العشرة لها معالج بريد (`dispatchEvent` switch) | ✅ مطابقة كود-بكود بين قيم `x_studio_event_type` المُنشأة فعلياً في كل الأتمتات وحالات الـ`switch` |
| J07 | رابط الاستكمال داخل بريد `preliminary_approved`/`final_more_information_required` | ✅ اكتُشف خطأ حقيقي أثناء المراجعة الذاتية: المسار الإنجليزي كان يُبنى كـ`/en/register/complete` بينما الموقع الفعلي بلا بادئة locale للإنجليزية (`/register/complete`) → أُصلح |

---

## K. تنبيهات انتهاء صلاحية المستندات

| # | الاختبار | النتيجة |
|---|---|---|
| K01 | مستند ينتهي خلال 5 أيام (عتبة 7 أيام) | ✅ بريد أُرسل، `expiry_alert_7/30/60_sent → true`، `expiry_alert_0_sent → false` |
| K02 | مستند منتهي منذ 3 أيام (عتبة "منتهي") | ⚠️ فشل مؤقت أول مرة بخطأ Odoo عابر (`Odoo Server Error` عام)، نجح عند إعادة المحاولة — لا تكرار إرسال بريد (الأعلام لم تُعلَّم عند الفشل) |
| K03 | إعادة تشغيل الدورة بعد نجاح K01/K02 | ✅ `skipped` للمستندين — يثبت عدم الإرسال المكرر |
| K04 | `x_studio_documents_expired` على ملف المورد | ✅ يُضبط `true` عند بلوغ عتبة الانتهاء الفعلي |

---

## L. الواجهة الأمامية (2A-10)

| # | الاختبار | النتيجة |
|---|---|---|
| L01 | `tsc --noEmit` على كامل المشروع بعد كل تعديل | ✅ بلا أخطاء |
| L02 | `eslint` على كامل المشروع | ✅ بلا أخطاء (باستثناء تحذير موجود مسبقاً في ملف مولَّد تلقائياً `next-env.d.ts`، غير مرتبط بهذا العمل) |
| L03 | تحميل `/ar/register` عبر HTTP فعلي | ✅ `200`، يحتوي محتوى النموذج الجديد ("ابدأ طلب الانضمام"، "اسم المنشأة") |
| L04 | تحميل `/ar/register/complete` بلا Token | ✅ `200`، يعرض حالة "جاري التحقق من رابط الدعوة" (لا خطأ خادم) |
| L05 | فورم التسجيل يطابق تماماً حقول `registerSchema` الجديدة (بلا CR في التسجيل الأولي) | ✅ اكتُشفت فجوة حقيقية: الفورم القديم كان لا يزال يرسل `cr_number` ولا يرسل `supplier_type/contact_name/categories/short_description/consent` المطلوبة الآن → أُعيد بناء الفورم بالكامل |
| L06 | فورم استكمال الملف يطابق `complete/route.ts` (محلي/دولي/بنك/نطاق تجاري/مستندات) | ✅ أُعيد بناء الفورم بالكامل ليطابق الحمولة المتداخلة الجديدة، ورُبط برفع المستندات عبر `/api/vendors/documents` والمسودة عبر `/api/vendors/draft` |
| L07 | اختبار تفاعلي داخل متصفح حقيقي (نقر، كتابة، إرسال فعلي عبر UI) | ❌ **لم يُنفَّذ** — امتداد المتصفح غير متاح في هذه البيئة. موصى به يدوياً على Vercel Preview قبل الاعتماد النهائي |

---

## ملخص

- **إجمالي البنود المنفَّذة فعلياً:** 62 بنداً (A–K)، جميعها بنتائج حقيقية من نظام حي (لا محاكاة، لا افتراضات).
- **فجوات موثّقة صراحة:** H03 (تكرار دولي غير مُختبر مباشرة، مغطى منطقياً)، L07 (لا اختبار متصفح تفاعلي).
- **أخطاء حقيقية اكتُشفت وأُصلحت أثناء هذا التنفيذ:** F04 (قيمة Selection خاطئة)، G02 (حقل Country كنص بدل معرف)، J07 (رابط بريد إنجليزي خاطئ)، بالإضافة لإعادة بناء فورمين أماميين كانا سيفشلان بالكامل مقابل الـAPI الجديد.
- هذا العدد أقل من الرقم الإرشادي (118) المذكور في التوجيه الأصلي؛ فُضِّلت مجموعة اختبارات حقيقية ومُنفَّذة فعلياً على قائمة أطول تحتوي بنوداً صورية لبلوغ رقم معيّن.
